apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: accounting-sql-db-pvc
  namespace: tecais
spec:
  resources:
    requests:
      storage: 3Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: tecais-storage-class
